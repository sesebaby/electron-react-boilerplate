# 最终重构计划

## 📋 重构目标

解决`待修复问题.md`第3项和第3.1项的核心问题：
- **第3.1项**：InventoryService过度工程化（1613行巨型类，10个Map缓存）
- **第3项**：双重数据库架构重复问题
- **总体目标**：遵循"单机版小型进销存，不要过度工程化"原则

## 🎯 重构方案

### 架构简化
```
当前架构（7层）: UI → Hook → ServiceManager → InventoryService → DatabaseManager → ElectronDatabase → IPC → SQLite
目标架构（4层）: UI → 领域服务 → 直接IPC调用 → SQLite
```

### 服务拆分
```
InventoryService (1613行) → 3个领域服务 (750行总计)
```

| 新服务 | 职责 | 代码行数 | 文件路径 |
|-------|------|---------|---------|
| InventoryDomainService | 产品+库存一体化管理 | 400行 | `src/services/domain/InventoryDomainService.ts` |
| MasterDataService | 分类、单位、仓库管理 | 200行 | `src/services/domain/MasterDataService.ts` |
| ReportService | 统计、报表、预警 | 150行 | `src/services/domain/ReportService.ts` |

## 📅 9天实施计划

### 第1天：数据库架构统一

#### 上午任务（4小时）
```bash
# 1. 备份数据库文件
cp inventory.db inventory.db.backup
cp data/inventory.db data/inventory.db.backup

# 2. 统一数据库路径
# 确保只使用 data/inventory.db
rm inventory.db  # 删除根目录重复文件
```

#### 下午任务（4小时）
- [ ] 更新 `public/main.js` 数据库路径配置
- [ ] 更新 `check_db.js` 检查路径
- [ ] 删除 `src/services/database/` 目录
- [ ] 验证应用启动和基础功能

#### 验收标准
- [ ] 只存在一个数据库文件：`data/inventory.db`
- [ ] 应用正常启动
- [ ] 基础CRUD操作正常

### 第2天：创建领域服务基础

#### 上午任务（4小时）
```bash
# 创建目录结构
mkdir -p src/services/domain
mkdir -p src/services/domain/__tests__
```

创建 `src/services/domain/types.ts`：
```typescript
export interface DomainServiceResult<T = any> {
  success: boolean;
  data?: T;
  error?: string;
}

export interface ProductWithStock {
  id: string;
  name: string;
  sku: string;
  currentStock: number;
  availableStock: number;
  minStock: number;
  maxStock: number;
  isLowStock: boolean;
  isOutOfStock: boolean;
  totalValue: number;
  // ... 其他字段
}

export interface StockOperationRequest {
  productId: string;
  warehouseId: string;
  quantity: number;
  unitPrice?: number;
  remark?: string;
  operator: string;
}
```

#### 下午任务（4小时）
- [ ] 实现 `InventoryDomainService.ts` 基础框架
- [ ] 实现 `getProductsWithStock` 方法
- [ ] 实现 `getProductStock` 方法
- [ ] 编写基础单元测试

#### 验收标准
- [ ] InventoryDomainService基础查询功能正常
- [ ] 产品和库存数据正确获取
- [ ] 单元测试覆盖率 > 70%

### 第3天：完善领域服务

#### 上午任务（4小时）
- [ ] 实现库存操作方法：`stockIn`、`stockOut`、`adjustStock`
- [ ] 实现事务性的 `executeStockOperation` 方法
- [ ] 添加产品CRUD方法：`createProduct`、`updateProduct`、`deleteProduct`

#### 下午任务（4小时）
- [ ] 实现 `MasterDataService.ts`（分类、单位、仓库管理）
- [ ] 实现 `ReportService.ts`（统计、报表功能）
- [ ] 创建 `DomainServiceManager.ts` 和向后兼容适配器

#### 验收标准
- [ ] 所有领域服务功能完整
- [ ] 库存事务处理正确
- [ ] 向后兼容性验证通过

### 第4天：Hook和核心组件迁移

#### 上午任务（4小时）
更新 `src/hooks/useInventory.ts`：
```typescript
// 原代码
const inventoryService = serviceManager.getInventoryService();
const result = await inventoryService.getProducts();

// 新代码
import { serviceManager } from '../services/domain/DomainServiceManager';
const inventoryService = serviceManager.getInventoryService(); // 返回InventoryDomainService
const result = await inventoryService.getProductsWithStock();
```

#### 下午任务（4小时）
- [ ] 更新 `ProductManagement.tsx` 组件
- [ ] 验证产品CRUD功能
- [ ] 测试数据显示和操作

#### 验收标准
- [ ] useInventory Hook功能正常
- [ ] ProductManagement组件工作正常
- [ ] 用户界面无回归问题

### 第5天：库存相关组件迁移

#### 上午任务（4小时）
更新 `src/components/Inventory/StockIn.tsx`：
```typescript
// 原代码
const result = await inventoryService.updateStock(productId, warehouseId, quantity, 'IN');

// 新代码
const result = await inventoryService.stockIn({
  productId,
  warehouseId,
  quantity,
  operator: 'system',
  remark: '入库操作'
});
```

#### 下午任务（4小时）
- [ ] 更新 `StockOut.tsx` 组件
- [ ] 更新 `InventoryList.tsx` 组件
- [ ] 功能回归测试

#### 验收标准
- [ ] 库存入库功能正常
- [ ] 库存出库功能正常
- [ ] 库存列表显示正确

### 第6天：报表和仪表板迁移

#### 上午任务（4小时）
- [ ] 更新 Dashboard 组件使用 ReportService
- [ ] 迁移统计信息显示
- [ ] 迁移预警信息显示

#### 下午任务（4小时）
- [ ] 更新报表相关组件
- [ ] 验证统计数据准确性
- [ ] 全面功能测试

#### 验收标准
- [ ] 仪表板数据显示正确
- [ ] 报表功能正常
- [ ] 统计信息准确

### 第7天：清理原有架构

#### 上午任务（4小时）
```bash
# 删除原有复杂架构
rm src/services/core/InventoryService.ts  # 删除1613行巨型类
rm -rf src/services/database/             # 删除重复数据库目录
```

#### 下午任务（4小时）
- [ ] 简化或删除复杂的 `ServiceManager.ts`
- [ ] 更新所有导入语句
- [ ] 代码清理和格式化

#### 验收标准
- [ ] 原有复杂代码完全移除
- [ ] 构建无错误无警告
- [ ] 所有功能仍然正常

### 第8天：性能优化和测试

#### 上午任务（4小时）
- [ ] 性能基准测试
- [ ] 内存使用测试
- [ ] 启动时间测试

#### 下午任务（4小时）
- [ ] 压力测试
- [ ] 并发操作测试
- [ ] 数据一致性测试

#### 验收标准
- [ ] 性能不低于重构前
- [ ] 内存使用减少50%以上
- [ ] 系统稳定性良好

### 第9天：最终验收

#### 上午任务（4小时）
- [ ] 全面功能验证
- [ ] 用户场景测试
- [ ] 边界条件测试

#### 下午任务（4小时）
- [ ] 用户验收测试
- [ ] 文档更新
- [ ] 部署准备

#### 验收标准
- [ ] 所有业务功能正常
- [ ] 用户体验无下降
- [ ] 文档完整准确

## 🛠️ 核心代码实现

### 1. InventoryDomainService 核心方法

```typescript
export class InventoryDomainService {
  // 获取产品及库存信息
  async getProductsWithStock(filter?: ProductFilter): Promise<DomainServiceResult<ProductWithStock[]>> {
    try {
      const query = `
        SELECT p.*, s.currentStock, s.availableStock, s.minStock, s.maxStock,
               (s.currentStock * p.salePrice) as totalValue
        FROM products p
        LEFT JOIN inventory_stocks s ON p.id = s.productId
        WHERE p.status != 'deleted'
      `;
      
      const products = await window.electronAPI.dbQuery(query);
      
      const productsWithStock = products.map(row => ({
        ...row,
        isLowStock: row.currentStock <= row.minStock,
        isOutOfStock: row.currentStock === 0
      }));
      
      return { success: true, data: productsWithStock };
    } catch (error) {
      return { success: false, error: error.message };
    }
  }

  // 库存入库操作（事务性）
  async stockIn(request: StockOperationRequest): Promise<DomainServiceResult<InventoryTransaction>> {
    try {
      await window.electronAPI.dbBeginTransaction();
      
      // 1. 验证产品和仓库存在
      const product = await window.electronAPI.dbGetProduct(request.productId);
      if (!product) throw new Error('产品不存在');
      
      // 2. 更新库存
      const currentStock = await this.getProductStock(request.productId, request.warehouseId);
      const newQuantity = currentStock.data.currentStock + request.quantity;
      
      await window.electronAPI.dbUpdateStock(request.productId, request.warehouseId, {
        currentStock: newQuantity,
        availableStock: newQuantity,
        updatedAt: new Date()
      });
      
      // 3. 创建交易记录
      const transaction = {
        id: uuidv4(),
        productId: request.productId,
        warehouseId: request.warehouseId,
        type: 'in',
        quantity: request.quantity,
        operator: request.operator,
        remark: request.remark,
        createdAt: new Date()
      };
      
      await window.electronAPI.dbCreateTransaction(transaction);
      await window.electronAPI.dbCommitTransaction();
      
      return { success: true, data: transaction };
    } catch (error) {
      await window.electronAPI.dbRollbackTransaction();
      return { success: false, error: error.message };
    }
  }

  // 向后兼容方法
  async getProducts(filter?: any): Promise<DomainServiceResult<{ items: ProductWithStock[] }>> {
    const result = await this.getProductsWithStock(filter);
    if (result.success) {
      return { success: true, data: { items: result.data } };
    }
    return result;
  }

  async updateStock(productId: string, warehouseId: string, quantity: number, type: string): Promise<DomainServiceResult<any>> {
    const request: StockOperationRequest = {
      productId,
      warehouseId,
      quantity,
      operator: 'system'
    };

    if (type === 'IN') return this.stockIn(request);
    if (type === 'OUT') return this.stockOut(request);
    if (type === 'ADJUST') return this.adjustStock(request);
    
    return { success: false, error: '不支持的操作类型' };
  }
}
```
